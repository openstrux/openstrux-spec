// generated by openstrux-generator — do not edit

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum ReviewStatus {
  pending
  under_review
  approved
  rejected
}

model Applicant {
  id               String @id @default(cuid())
  email            String @unique
  name             String
  orgId            String
  org              LegacyOrg @relation(fields: [orgId], references: [id])
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt
  @@map("applicants")
  proposals        Proposal[]
}

model Proposal {
  id               String @id @default(uuid())
  title            String
  authorId         String
  author           Applicant @relation(fields: [authorId], references: [id], onDelete: Cascade)
  status           ReviewStatus @default("pending")
  archived         Boolean @default(false)
  internalId       String @map("internal_ref") @ignore
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt
  @@index([authorId, status])
  @@unique([title, authorId])
  // @opaque index("idx_proposals_fulltext",[title])
  reviewEvents     ReviewEvent[]
}

model ReviewEvent {
  id               String @id @default(cuid())
  proposalId       String
  proposal         Proposal @relation(fields: [proposalId], references: [id], onDelete: Cascade)
  reviewerId       String
  occurredAt       DateTime @default(now())
  lastUpdate       DateTime @updatedAt
}
